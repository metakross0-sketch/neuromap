# NeuroMap TMA

Живая нейронная карта магазинов России для Telegram Mini App.

## Концепция

Карта представлена как **живой организм** - нейронная сеть, где:
- 🧠 **Магазины = нейроны** с разным уровнем активности
- ⚡ **Связи = синапсы** между близкими точками
- 🌊 **Волны активности** распространяются при взаимодействии
- 💫 **Пульсация** показывает живую активность системы

## Технологии

- **Preact 10** - легкий React-like фреймворк (3KB)
- **TypeScript** - типизация и надежность
- **Zustand** - легкий state manager (1KB)
- **Canvas 2D API** - рендеринг нейросети (60 FPS)
- **Vite** - молниеносный dev-сервер и сборка

## Установка

```bash
npm install
npm run dev
```

## API

Интеграция с Google Sheets через Flask API:
- `GET /api/cities` - список городов
- `GET /api/shops/:city` - магазины города
- `GET /api/shop/:id/catalog` - каталог магазина
- `GET /api/categories/:city` - категории по городу

## Структура

```
веб3/
├── src/
│   ├── components/     # Preact компоненты
│   ├── store/          # Zustand стор
│   ├── api/            # API клиент
│   └── types.ts        # TypeScript типы
├── index.html
├── package.json
└── vite.config.ts
```

## Запуск

1. Установите Node.js (если нет)
2. `npm install`
3. `npm run dev`
4. Откройте http://localhost:5173

## Деплой

```bash
npm run build
# Загрузите dist/ в Telegram Mini App
```

## Возможности

✅ Canvas-рендеринг нейронной сети  
✅ Анимация волн активности  
✅ Touch-оптимизация для мобильных  
✅ Интеграция с Telegram Web App API  
✅ Минимальный бандл (<800KB)  
✅ Offline-ready архитектура
