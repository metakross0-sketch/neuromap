# 🗺️ СИСТЕМА СТАТИЧЕСКИХ GEOJSON ФАЙЛОВ

## Что это?

Вместо медленной загрузки дорог с Overpass API (30-60 сек), используем предварительно загруженные GeoJSON файлы на GitHub Pages (0.5-2 сек).

---

## 📋 Инструкция по использованию

### 1. Генерация файлов (делать на своем ПК)

```bash
# Установи зависимости если нужно
pip install requests

# Запусти скрипт генерации
python generate_roads.py
```

**Скрипт:**
- Получит список городов с твоего Flask API
- Для каждого города загрузит дороги из Overpass API
- Сохранит файлы в `public/roads/tyumen.geojson`, `public/roads/moskva.geojson` и т.д.
- Займет 5-15 минут в зависимости от количества городов

### 2. Деплой

```bash
npm run build
npm run deploy
```

Файлы из `public/roads/` автоматически попадут на GitHub Pages.

---

## 🚀 Как это работает

**Фронтенд (MapView.tsx):**
1. Пробует загрузить `/roads/tyumen.geojson` с GitHub Pages (быстро!)
2. Если файла нет → fallback на Flask API (медленно)

**Преимущества:**
- ⚡ Загрузка 0.5-2 сек вместо 30-60 сек
- 💰 Бесплатно (GitHub Pages CDN)
- 🌍 Быстро по всему миру
- ✅ Все эффекты (неон, маршруты, затемнение) работают как раньше

**Обновление данных:**
- Запускай `python generate_roads.py` раз в месяц/квартал
- Потом `npm run deploy`

---

## 📁 Структура файлов

```
веб3/
├── generate_roads.py          # Скрипт генерации
├── public/
│   └── roads/
│       ├── tyumen.geojson     # ~15-30 МБ
│       ├── moskva.geojson     # ~40-80 МБ
│       └── ...
└── src/
    └── components/
        └── MapView.tsx         # Обновленный код загрузки
```

---

## ⚠️ Важно

- Файлы большие (~10-50 МБ каждый)
- GitHub Pages лимит 100 МБ на файл - OK
- Для 5-10 городов = ~100-300 МБ total - OK
- Первая загрузка пользователем = 1-2 сек (CDN закэширует)

---

## 🔧 Следующие шаги

1. Запусти `python generate_roads.py` СЕЙЧАС
2. Обнови код фронтенда (я дам правки)
3. `npm run deploy`
4. Наслаждайся мгновенной загрузкой! 🎉
